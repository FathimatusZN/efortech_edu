/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "source.unsplash.com",
      "storage.googleapis.com",
      "localhost",
      "edu.efortechsolutions.com",
    ],
  },
};

module.exports = nextConfig;
